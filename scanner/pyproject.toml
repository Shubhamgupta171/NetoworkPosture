[project]
name = "nps-scanner"
version = "0.1.0"
description = "Network Posture Scanner CLI"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.setuptools.packages.find]
where = ["src"]
